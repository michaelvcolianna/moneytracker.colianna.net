<a
    href="{{ $href }}"
    rel="{{ $rel }}"
    aria-label="{{ $aria }} date: {{ $label }}"
    class="icon icon--chevrons-{{ $arrow }}"
>
    <time datetime="{{ $rfc }}">
        {{ $time }}
    </time>
</a>
